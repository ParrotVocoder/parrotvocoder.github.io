<footer class="site-footer">
  <p class="visitor-counter">
    Number of visitors: <span id="stats">Loading...</span>
  </p>
  <p class="tagline">Arielle understands speech and speaks thoughtfully using English words, phrases, and sentences.</p>
  <p class="copyright">
    &copy; {new Date().getFullYear()} ParrotSpeech.org - All Rights Reserved
  </p>
</footer>

<script is:inline>
  ;(() => {
    const statsEl = document.querySelector("#stats")
    if (!statsEl) return

    // Encode path for GoatCounter — strip trailing slash except for root
    function getCounterPath() {
      let path = location.pathname

      // Prefer GoatCounter's own path if available
      if (window.goatcounter && typeof window.goatcounter.get_data === "function") {
        const data = window.goatcounter.get_data()
        if (data && data.p) path = data.p
      }

      // Normalize: remove trailing slash unless it's the root "/"
      if (path.length > 1 && path.endsWith("/")) {
        path = path.slice(0, -1)
      }

      if (path.startsWith("/")) {
        path = path.slice(1)
      }

      return encodeURIComponent(path)
    }

    const url = "https://parrotspeech.goatcounter.com/counter/" + getCounterPath() + ".json"

    const r = new XMLHttpRequest()

    r.addEventListener("load", function () {
      if (this.status < 200 || this.status >= 300) {
        statsEl.innerText = "N/A"
        return
      }

      try {
        const payload = JSON.parse(this.responseText)
        const count = Number.parseInt(String(payload?.count ?? ""), 10)
        statsEl.innerText = Number.isFinite(count) ? String(count) : "0"
      } catch {
        statsEl.innerText = "N/A"
      }
    })

    r.addEventListener("error", () => {
      statsEl.innerText = "N/A"
    })

    r.open("GET", url)
    r.send()
  })()
</script>

<style>
  .site-footer {
    margin-top: 40px;
    padding: 30px 20px;
    text-align: center;
    border-top: 2px solid #009966;
  }

  .visitor-counter {
    font-size: 1rem;
    color: #1f4d2e;
    margin: 0 0 12px;
  }

  .tagline {
    font-size: 1.4rem;
    color: #333;
    margin: 0 0 15px;
    font-style: italic;
  }

  .copyright {
    font-size: 0.9rem;
    color: #666;
    margin: 0;
  }
</style>
